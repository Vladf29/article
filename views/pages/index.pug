extends ../layout

block append head
    -var titlePage = 'Main page'
    link(rel="stylesheet", href="/styles/index.css")

block content
    .hero
        .hero__c
            if(img)
                .hero__icon
                    img(src=img, alt="")
            h1.hero__title=topic  
    include ../parts/post


block append script
    script(src="/js/index.js")